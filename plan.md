Instead of using that jank "IIXP" thing (`/src/iixp.pyx`), I actually read the TLS (terrifying!), and I wonder if you can "easily" do mTLS+Ed25519/X25519+RPK (Raw Public Keys) w/ a libssl shim? I don't mind playing with raw TCP sockets. And, no, you can't cheat with faking X.509 compliance; X.509 is bloated and much too heavy, also you should know how 99% of libraries treat self-signed certs, that's before we even talk about firewall/OS-level configurations. You're gonna have to take the hard route and implement RPK with a thin libssl shim.
